package com.terra.pjatk.aug.grammar.context;

public enum ExpressionType {
    NUMBER,
    STRING,
    ASSIGN,
    NUMBER_RELATION,
    STRING_RELATION,
    BOOL,
    IF_STATEMENT,
    INSTRUCTION,
    INSTRUCTION_BLOCK,
    FOR_LOOP_STATEMENT,
    OUTPUT
}
